# LM Studio 中文深度汉化补丁 0.4.20-1 deep1.1

适用版本：LM Studio `0.4.20+1` Windows x64 正式版。

深度版包含安全版 v2 的 87 项官方词库补全，并增加 227 项硬编码界面翻译和动态控件翻译规则，覆盖：

- 聊天页、侧栏和工具提示；
- 应用设置、模型默认设置、运行时与硬件设置；
- 模型搜索窗口的固定控件；
- LM Link 固定界面和提示；
- 开发者页面导航、本地服务器及 API 功能说明。

## 安装

1. 完全退出 LM Studio，包括系统托盘中的进程。
2. 双击本包的 `01_install_patch.cmd`。
3. 出现 Windows 用户账户控制提示时选择“是”。
4. 启动 LM Studio，并在设置中选择“简体中文”。

deep1.1 支持从官方原版直接安装，也支持从“中文增强补丁 v2”自动升级。检测到 v2 时，脚本会先校验 v2 文件和它保存的官方原版备份，再用该备份生成深度版；不需要手工恢复。

脚本只接受官方原版 `0.4.20+1` 文件。原文件、候选文件、备份任一哈希不符都会停止，不会跨版本强行修改。

## 恢复

完全退出 LM Studio，双击 `02_restore_original.cmd`。

深度版备份位置：

`resources\app\.webpack\renderer\main_window.js.zh-cn-deep-0.4.20-1.bak`

## 翻译边界

- LM Studio、LM Link、Hugging Face、模型名称、API 路径、参数名和技术缩写保留原文。
- 远端模型库中的模型介绍由模型作者动态提供，不做固定替换。
- 代码块、命令、JSON、路径和可编辑输入区域不会被界面翻译层修改。
- 深度版只支持简体中文使用场景；切换其他语言后应重新启动 LM Studio。
- 软件更新后必须先恢复原版。本补丁只支持 `0.4.20+1`。

## 文件哈希

- 官方原版 `main_window.js`：
  `5BEC77399EBD6C5C1B5426F506949C6D3202AAE0F9545B4E0BCCAB56EC7C788B`
- 深度汉化后 `main_window.js`：
  `A47D4DABEFA207A5699223B9A0BADF2CF9A29664154D8153EB6387BDEA285867`
